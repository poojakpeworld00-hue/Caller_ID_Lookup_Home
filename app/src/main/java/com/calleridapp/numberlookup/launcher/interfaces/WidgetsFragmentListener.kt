package com.calleridapp.numberlookup.launcher.interfaces

import com.calleridapp.numberlookup.launcher.models.AppWidget

interface WidgetsFragmentListener {
    fun onWidgetLongPressed(appWidget: AppWidget)
}
